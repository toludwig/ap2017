
/*
less(z,s(_)).
less(s(X1),s(X2)) :-
  less(X1, X2).

select2(X,Xs,R) :-
  select1(X,Xs,R);
  R = Xs.

count([], z).
count([_|T], s(I)) :-
  count(T, I).

member(X, G) :-
  elem(person(X, _), G).

not_in(X, L) :-
  select2(X, L, R),
  count(L, C1),
  count(R, C2),
  equal(C1, C2).
*/
